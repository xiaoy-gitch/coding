题目：

给你一个 正 整数数组 nums 。
请你求出 nums 中有多少个子数组，满足子数组中 第一个 和 最后一个 元素都是这个子数组中的 最大值。

思路：

【第一个和最后一个元素都是子数组中的最大值】意味着子数组的首尾元素必须相等。
要保证第一个和最后一个的元素最大，则要维护一个单调栈，保证栈中的元素单调递减，因为一旦出现大于栈顶值的元素，则栈顶值一定不能作为边界节点，因为它的两边都是比它大的元素。所以可以直接将栈顶元素出栈。
栈中每个元素是 [数值, 该数值连续出现的次数]
